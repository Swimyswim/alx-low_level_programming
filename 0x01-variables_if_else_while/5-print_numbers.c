#include <stdio.h>

/**
 * main - print all single digit number of base 10
 * starting from 0
 * Return: Always 0 (Success)
 */

int main(void)
{
	int a;

	for (a = 0; a < 10; a++)

		printf("%d", a);
	printf("\n");
	return (0);
}
