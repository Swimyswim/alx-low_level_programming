#include "main.h"

/**
 * main - prints 10 times the alphabet in lowercase
 */

void print_alphabet_x10(void)
{
	char al;
	int i;

	i = 0
		while (1 < 10)
		{
			al = 'a';
			while (al <= 'z')
			{
				_putchar(al);
				al++
			}
			_putchar('\n');
			i++;
		}
}
