#include <stdlib.h>
#include <stdio.h>

/**
 * main - converst character to strings
 * %d: value
 * i: calue
 * @*s: value
 * Return: results
 */

int main(void)
{
	int i;
	char *s;

	s = " -9885";
	i = atoi(s);     /* i = -9885 */

	printf("i = %d\n", i);

}
